import {User} from './user';

export class Customer {
  customerId: number;
  name: string;
  lastname: string;
  email: string;
  phoneNumber: string;
  facebookAcc: string;
  address: string;
  user: User;
}
